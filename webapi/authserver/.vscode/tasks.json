{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "buildauthserver",
      "command": "dotnet",
      "type": "process",
      "args": ["build", "${workspaceFolder}/authserver.csproj"],
      "problemMatcher": "$msCompile"
    },
    {
      "label": "build",
      "dependsOn": ["buildauthserver"]
    }
  ]
}
